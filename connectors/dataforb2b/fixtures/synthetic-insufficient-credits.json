{
    "name": "synthetic-insufficient-credits",
    "description": "The balance does not cover the worst case of the request: 402 with the object detail every endpoint can answer with. Vendor non-2xx is DATA, settled at zero. Synthetic, shaped on the credit pre-check (2026-09-23).",
    "calls": [
        {
            "req": {
                "method": "POST",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 402,
                "body": {
                    "detail": {
                        "error": "Insufficient credits",
                        "available_credits": 4,
                        "required_credits": 37.5,
                        "cost_per_result": 1.5
                    }
                }
            }
        }
    ]
}
